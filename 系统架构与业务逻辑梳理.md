# 学生选课系统 - 架构与业务逻辑梳理

## 一、学生选课完整链路

### 1.1 整体架构分层

```
前端 (Vue3 + Element Plus)
    ↓ HTTP 请求
后端 Controller 层 (Spring REST API)
    ↓ 业务调用
后端 Service 层 (业务逻辑)
    ↓ 数据持久化
后端 Repository 层 (Spring Data JPA)
    ↓ SQL 执行
MySQL 数据库
```

### 1.2 选课操作完整调用链

#### 前端触发（学生视角）
1. **页面**: `frontend/src/views/Course.vue`
   - 学生登录后进入"可选课程"页面
   - 点击课程行的"选课"按钮
   - 调用 `handleSelectCourse(row)` 方法

2. **API 请求**: `frontend/src/api/request.js`
   - 发送 `POST /api/selections` 请求
   - 请求体: `{ student: { id: userId }, course: { id: courseId } }`
   - `userId` 从 `localStorage` 获取

#### 后端处理
3. **Controller 层**: `backend/src/main/java/com/example/courseselection/controller/SelectionController.java:28-31`
   ```java
   @PostMapping
   public Selection create(@RequestBody Selection selection) {
       return selectionService.save(selection);
   }
   ```
   - 接收 `Selection` 实体（包含 student 和 course 的 id 关联）
   - 直接调用 Service 层，无业务校验

4. **Service 层**: `backend/src/main/java/com/example/courseselection/service/SelectionService.java:29-31`
   ```java
   public Selection save(Selection selection) {
       return selectionRepository.save(selection);
   }
   ```
   - 目前是透传调用，无业务逻辑校验

5. **Repository 层**: `SelectionRepository extends JpaRepository<Selection, Long>`
   - Spring Data JPA 自动实现持久化
   - 生成 `INSERT INTO selection` SQL

#### 数据库层
6. **数据表**: `selection` 表
   - 字段: `id`, `student_id`, `course_id`, `grade`
   - 唯一约束: `UNIQUE KEY unique_selection (student_id, course_id)`
   - 确保同一学生不能重复选同一门课

### 1.3 退课操作完整调用链

#### 前端触发
1. **页面**: `frontend/src/views/Selection.vue`
   - 学生在"我的选课"页面点击"退选"按钮
   - 调用 `handleDelete(row)` 方法
   - 前端已有简单校验: `:disabled="scope.row.grade !== null"`（成绩已录入则不能退选）

2. **API 请求**: 发送 `DELETE /api/selections/{id}`

#### 后端处理
3. **Controller 层**: `SelectionController.java:39-42`
   ```java
   @DeleteMapping("/{id}")
   public void delete(@PathVariable Long id) {
       selectionService.deleteById(id);
   }
   ```

4. **Service 层**: `SelectionService.java:33-35`
   ```java
   public void deleteById(Long id) {
       selectionRepository.deleteById(id);
   }
   ```

5. **Repository 层**: JPA 生成 `DELETE FROM selection WHERE id = ?`

---

## 二、接口鉴权机制分析

### 2.1 鉴权方式结论

**当前系统既不是标准 JWT，也不是 Session，而是一种非常简化的自定义 Token 机制（且后端未做实际校验）。**

### 2.2 登录流程详解

#### 前端登录
- **页面**: `frontend/src/views/Login.vue`
- 提交用户名密码到 `POST /api/auth/login`

#### 后端登录处理
- **Controller**: `backend/src/main/java/com/example/courseselection/controller/AuthController.java:24-59`
- 登录逻辑:
  1. 先尝试管理员登录: 查询 `admin` 表，匹配用户名密码
  2. 若失败，尝试学生登录: 查询 `student` 表，匹配学号和密码
  3. 登录成功返回:
     ```json
     {
       "token": "admin-token-{id}" 或 "student-token-{id}",
       "username": "xxx",
       "role": "admin" 或 "student",
       "userId": "{id}"  // 学生登录时返回
     }
     ```

#### Token 存储与使用
- **前端存储**: `localStorage.setItem('token', res.token)`
- **前端路由守卫**: `frontend/src/router/index.js:58-74`
  ```javascript
  router.beforeEach((to, from, next) => {
    const token = localStorage.getItem('token')
    const role = localStorage.getItem('role')
    // 1. 非登录页且无 token → 跳登录
    // 2. 路由配置了 roles 但当前角色不在其中 → 无权限跳转
  })
  ```

### 2.3 鉴权机制的问题

1. **后端无接口鉴权**: 所有 `@RestController` 都没有 Token 校验拦截器
   - 没有 `HandlerInterceptor` 或 `Filter`
   - 没有 Spring Security 配置
   - 任何人只要知道 API 地址，都可以直接调用

2. **Token 无意义**: Token 只是简单的字符串拼接，没有签名、过期时间
   - 无法校验 Token 真伪
   - 无法做 Token 过期管理

3. **前端路由守卫仅作页面跳转控制**: 不能真正保护后端接口

---

## 三、新增功能：只允许在开课前3天内退课

### 3.1 现有系统缺失的前提

**当前 `Course` 实体没有开课时间字段！**

```java
// Course.java 当前只有:
private Long id;
private String courseNumber;
private String name;
private Integer credits;
```

要实现"开课前3天内退课"，首先需要知道课程什么时候开课。

### 3.2 需要改动的模块清单

#### 【数据库层】
1. **修改 `course` 表结构**
   - 新增 `start_date` 字段（DATE 类型）
   - SQL: `ALTER TABLE course ADD COLUMN start_date DATE;`
   - 需为已有课程数据补全开课日期

#### 【实体层】
2. **修改 `Course.java` 实体**
   - 新增字段: `private LocalDate startDate;`
   - JPA 自动映射（需确保 `spring.jpa.hibernate.ddl-auto` 配置正确）

#### 【前端 - 课程管理】
3. **修改 `Course.vue` 课程表单**
   - 添加课程/编辑课程时增加"开课日期"选择器
   - 列表页可显示开课日期（可选）

#### 【前端 - 选课管理】
4. **修改 `Selection.vue` 退选逻辑（可选增强）**
   - 可在前端提前判断并禁用退选按钮，提升用户体验
   - 但**必须以后端校验为准**，前端校验仅为体验优化

#### 【后端 - 退课校验】
5. **修改 `SelectionService.java` 的 `deleteById` 方法**
   - 在删除前增加业务校验逻辑:
     ```
     伪代码:
     1. 根据 selectionId 查询 Selection 记录
     2. 通过 Selection 获取关联的 Course
     3. 获取 Course.getStartDate()
     4. 计算: startDate.minusDays(3) 与 当前日期 比较
     5. 如果 当前日期 > startDate.minusDays(3) → 不允许退课，抛异常
     6. 否则 → 执行删除
     ```

#### 【后端 - 异常处理（可选优化）】
6. **自定义业务异常**
   - 新增 `BusinessException extends RuntimeException`
   - 包含错误码和消息
   
7. **完善 `GlobalExceptionHandler.java`**
   - 捕获 `BusinessException`
   - 返回 HTTP 400 + 友好错误信息: "距离开课不足3天，无法退课"

### 3.3 改动优先级建议

| 优先级 | 改动点 | 原因 |
|--------|--------|------|
| P0 | 数据库 course 表加 start_date 字段 | 功能前提 |
| P0 | Course 实体加 startDate 字段 | 功能前提 |
| P0 | SelectionService.deleteById 加校验逻辑 | 核心业务规则 |
| P0 | 课程编辑表单加开课日期录入 | 数据录入入口 |
| P1 | 前端退选按钮前置校验 | 优化用户体验 |
| P2 | 自定义异常 + 全局异常处理 | 代码可维护性 |

### 3.4 校验逻辑伪代码（后端）

```java
// SelectionService.java
public void deleteById(Long id) {
    Selection selection = selectionRepository.findById(id)
        .orElseThrow(() -> new IllegalArgumentException("选课记录不存在"));
    
    Course course = selection.getCourse();
    LocalDate startDate = course.getStartDate();
    LocalDate today = LocalDate.now();
    
    // 计算: 退课截止时间 = 开课日期 - 3天
    LocalDate dropDeadline = startDate.minusDays(3);
    
    // 如果今天已经过了退课截止时间
    if (today.isAfter(dropDeadline)) {
        throw new BusinessException(
            "距离开课不足3天（开课日期: " + startDate + "），无法退课"
        );
    }
    
    selectionRepository.deleteById(id);
}
```

---

## 四、架构总结

### 4.1 当前系统特点
- **简单直接**: 典型的 MVC 三层架构，没有复杂的中间件
- **前后端分离**: Vue + Spring Boot，通过 REST API 交互
- **数据层**: JPA + MySQL，有基础的外键关联和唯一约束
- **鉴权薄弱**: 仅前端路由控制，后端接口完全暴露

### 4.2 关键文件索引
| 功能 | 文件路径 |
|------|----------|
| 选课API | `backend/.../controller/SelectionController.java` |
| 选课业务 | `backend/.../service/SelectionService.java` |
| 登录认证 | `backend/.../controller/AuthController.java` |
| 学生选课页 | `frontend/src/views/Course.vue` |
| 我的选课页 | `frontend/src/views/Selection.vue` |
| 路由守卫 | `frontend/src/router/index.js` |
| 数据库脚本 | `backend/init.sql` |
