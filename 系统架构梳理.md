# 学生选课系统架构梳理

## 一、学生完成选课的完整链路

### 1. 整体架构概览

```
浏览器 (Vue3 + Element Plus)
    │
    ▼  HTTP 请求 (经由 Nginx 反向代理 /api → backend:8080)
Nginx (frontend 容器, 端口 3000 → 80)
    │
    ▼
Spring Boot 后端 (端口 8080)
    │
    ▼  JPA / Hibernate
MySQL 数据库 (端口 3306)
```

### 2. 选课链路详细流程（以学生选课为例）

#### 第一层：前端发起请求

**入口文件**：`frontend/src/views/Course.vue`

1. 学生在「可选课程」页面点击「选课」按钮
2. 触发 `handleSelectCourse(row)` 方法（`Course.vue:142`）
3. 通过 `request.js` 封装的 axios 实例发送 `POST /api/selections` 请求
4. 请求体为 `{ student: { id: userId }, course: { id: row.id } }`

**关键代码路径**：
```
Course.vue → request.post('/selections', payload)
                     ↓
         api/request.js (axios 实例, baseURL: '/api')
```

#### 第二层：Nginx 反向代理

**配置文件**：`frontend/nginx.conf`

- 前端容器运行 Nginx，监听 80 端口
- `/api` 路径的请求被反向代理到 `http://backend:8080`
- 前端静态资源由 Nginx 直接返回

#### 第三层：Spring Boot Controller 层

**入口文件**：`backend/.../controller/SelectionController.java`

1. 请求到达 `SelectionController` 的 `create()` 方法（`SelectionController.java:29`）
2. 接收 `@RequestBody Selection` 对象
3. 直接调用 `selectionService.save(selection)`

> ⚠️ **注意**：Controller 层没有任何参数校验、重复选课检查或业务逻辑，直接透传到 Service。

#### 第四层：Service 层

**入口文件**：`backend/.../service/SelectionService.java`

1. `save()` 方法（`SelectionService.java:29`）直接调用 `selectionRepository.save(selection)`
2. Service 层同样没有额外业务逻辑，是纯粹的透传层

> ⚠️ **注意**：Service 层未实现重复选课校验、容量限制等任何业务规则。

#### 第五层：Repository (JPA) 层

**入口文件**：`backend/.../repository/SelectionRepository.java`

1. 继承 `JpaRepository<Selection, Long>`
2. 调用默认的 `save()` 方法
3. Spring Data JPA 通过 Hibernate 生成 SQL

#### 第六层：数据库写入

**表结构**：`selection` 表（`init.sql:35`）

```sql
CREATE TABLE selection (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    student_id BIGINT NOT NULL,
    course_id BIGINT NOT NULL,
    grade DOUBLE,
    FOREIGN KEY (student_id) REFERENCES student(id),
    FOREIGN KEY (course_id) REFERENCES course(id),
    UNIQUE KEY unique_selection (student_id, course_id)
);
```

- 唯一约束 `unique_selection (student_id, course_id)` 防止同一学生重复选同一门课
- 如果违反唯一约束，会抛出 `DataIntegrityViolationException`，被 `GlobalExceptionHandler` 捕获返回 409 错误

### 3. 退课链路

**前端入口**：`Selection.vue:202` → `handleDelete(row)`

1. 学生在「我的选课」页面点击「退选」按钮
2. 前端判断：`scope.row.grade !== null` 时禁用退选（已有成绩不可退）
3. 发送 `DELETE /api/selections/{id}`
4. 经过 Nginx → `SelectionController.delete()` → `SelectionService.deleteById()` → `SelectionRepository.deleteById()`
5. 直接从数据库删除记录，无任何时间窗口校验

### 4. 链路总结图

```
[学生点击「选课」]
       │
       ▼
Course.vue (handleSelectCourse)
       │  POST /api/selections  {student: {id}, course: {id}}
       ▼
api/request.js (axios, baseURL=/api)
       │
       ▼
Nginx 反向代理 (/api → backend:8080)
       │
       ▼
SelectionController.create(@RequestBody Selection)
       │
       ▼
SelectionService.save(selection)
       │
       ▼
SelectionRepository.save(selection)  [JpaRepository]
       │
       ▼
Hibernate → INSERT INTO selection (student_id, course_id, grade)
       │
       ▼
MySQL (唯一约束防重复选课)
```

---

## 二、现有接口鉴权机制分析

### 结论：既不是 JWT，也不是 Session，是伪 Token + 前端本地存储

### 1. 登录流程

**后端**：`AuthController.java:25` → `login()` 方法

1. 接收用户名和密码（明文传输）
2. 先查 `Admin` 表，再查 `Student` 表
3. 密码直接明文比对（`admin.getPassword().equals(loginRequest.getPassword())`）
4. 登录成功后返回一个**硬编码字符串**作为 token：
   - 管理员：`"admin-token-" + admin.getId()`（如 `admin-token-1`）
   - 学生：`"student-token-" + student.getId()`（如 `student-token-2`）

```java
response.put("token", "admin-token-" + admin.getId());
response.put("role", "admin");
```

### 2. Token 存储与使用

**前端存储**：`Login.vue:99`

```javascript
localStorage.setItem('token', res.token)
localStorage.setItem('role', res.role)
localStorage.setItem('userId', res.userId)
```

- token、role、userId 全部存储在 `localStorage`
- **后端从不验证 token**：没有 SecurityConfig、没有 Filter、没有 Interceptor
- 前端 axios 实例（`request.js`）**没有在请求头中携带 token**

### 3. 路由守卫

**前端路由守卫**：`router/index.js:58`

```javascript
router.beforeEach((to, from, next) => {
  const token = localStorage.getItem('token')
  if (to.path !== '/login' && !token) {
    next('/login')
  } else if (to.meta.roles && !to.meta.roles.includes(role)) {
    // 角色不匹配，跳转默认页
  }
})
```

- 仅在前端判断 `localStorage` 中是否有 token 和 role
- **后端所有 API 完全裸奔**，任何人直接调用 `/api/selections` 等接口都能访问

### 4. 鉴权问题总结

| 问题 | 现状 |
|------|------|
| Token 生成 | 硬编码拼接字符串，无加密、无签名、无过期 |
| Token 验证 | **后端不验证**，无 Filter/Interceptor |
| 密码存储 | 明文存储在数据库 |
| 密码传输 | 前端明文传输（无 HTTPS 强制） |
| 请求鉴权 | axios 未在 Header 中携带 token |
| API 安全 | 所有接口完全公开，可直接调用 |
| 角色控制 | 仅前端路由守卫，后端无角色校验 |

---

## 三、实现"只允许开课前3天内退课"功能的影响分析

### 1. 核心前提：Course 实体缺少「开课时间」字段

当前 `Course` 实体只有 `courseNumber`、`name`、`credits` 三个字段，**没有开课时间（startDate）**。这是实现该功能的前提条件。

### 2. 需要改动的位置

#### (1) 数据库层

| 改动项 | 说明 |
|--------|------|
| `course` 表 | 新增 `start_date DATE` 字段，记录课程开课日期 |
| `init.sql` | INSERT 语句中为已有课程补充开课日期数据 |

#### (2) 后端 Entity 层

| 文件 | 改动 |
|------|------|
| `Course.java` | 新增 `private LocalDate startDate;` 字段 |

#### (3) 后端 Service 层（核心逻辑）

| 文件 | 改动 |
|------|------|
| `SelectionService.java` | 在 `deleteById()` 方法中添加退课时间校验逻辑 |

**校验逻辑伪代码**：

```
1. 根据 selectionId 查出 Selection 记录
2. 通过 selection.course 获取 Course 对象
3. 计算 course.startDate 与当前日期的差值
4. 若 差值 > 3天（即开课前超过3天），拒绝退课
5. 若 差值 ≤ 3天（含已开课后），允许退课
6. 抛出自定义异常，由 GlobalExceptionHandler 统一处理返回错误提示
```

#### (4) 后端异常处理

| 文件 | 改动 |
|------|------|
| `GlobalExceptionHandler.java` | 新增自定义异常的处理器，返回 403/400 和友好提示 |

或者新增一个自定义异常类，如 `DropCourseNotAllowedException`。

#### (5) 前端展示层

| 文件 | 改动 |
|------|------|
| `Selection.vue` | 退选按钮增加禁用条件：若开课前超过3天，禁用退选按钮并显示提示 |
| `Course.vue` | 课程管理/可选课程页面，展示开课日期字段 |
| 课程新增/编辑对话框 | 新增「开课日期」表单项 |

#### (6) 前端 API 响应处理

| 文件 | 改动 |
|------|------|
| `Selection.vue` | `handleDelete` 的 catch 中处理后端返回的退课时间限制错误提示 |

### 3. 改动影响范围图

```
数据库
  └─ course 表新增 start_date 列
        │
Entity 层
  └─ Course.java 新增 startDate 字段
        │
Service 层 (核心)
  └─ SelectionService.deleteById() 增加时间校验
        │
Exception 层
  └─ GlobalExceptionHandler 新增异常处理
        │
前端展示层
  ├─ Selection.vue  → 退选按钮禁用逻辑 + 错误提示
  ├─ Course.vue     → 展示开课日期 + 新增/编辑表单
  └─ request.js     → 无需改动
```

### 4. 需要注意的边界情况

| 边界场景 | 处理建议 |
|----------|----------|
| 开课当天（差值=0天） | 应允许退课 |
| 已开课后（差值为负数） | 根据业务需求决定是否允许退课 |
| Course.startDate 为 null | 需兼容旧数据，可设为不限制退课 |
| 管理员删除选课记录 | 管理员操作是否受此限制？建议不受限 |
| 时区问题 | 服务端统一使用 UTC 或服务器本地时间，避免客户端时区差异 |

### 5. 可选的增强方案

- **配置化**：将"3天"做成应用配置项（`application.properties`），而非硬编码
- **数据库触发器**：在数据库层也加一层约束，防止绕过应用层直接操作数据库
- **前端优化**：选课列表中增加「退课截止日期」列，提升用户体验
